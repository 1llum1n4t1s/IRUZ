# IRUZ 設計

## 目的と範囲

IRUZ は Windows の最終入力時刻を定期的に更新し、Teams などの離席判定をリセットする常駐型マウスジグルアプリである。ユーザー操作を邪魔しない相対マウス移動、一定時間後の自動解除、タスクトレイ操作、Windows ログイン時の起動、Velopack による自己更新を一つの Windows x64 アプリとして提供する。

ランディングページは同じホスト名で配信するが、デスクトップアプリとは別の Cloudflare Worker である。開発・検証・リリースの具体的な作業規約は [AGENTS.md](AGENTS.md) を参照する。

## 主要コンポーネント

| コンポーネント | 責務 | 境界 |
| --- | --- | --- |
| `Program` | Velopack 初期化、単一インスタンス、更新確認と適用、別プロセスからの復帰通知 | Avalonia の UI 構築前に完結するプロセス制御 |
| `App` | メインウィンドウ、トレイアイコン、終了条件、購読とリソースのライフタイム | ViewModel の要求を実際のウィンドウ／アプリ操作へ変換 |
| `MainWindow` | XAML の初期化、起動時最小化、アクリルのフォールバック接続 | 表示固有の処理だけを持ち、状態は ViewModel へ委譲 |
| `MainWindowViewModel` | ジグルの開始・停止、間隔、自動解除、状態表示、コマンド、スタートアップ設定 | メイン画面とトレイメニューが共有する状態の正本 |
| `MouseJiggleHelper` | `user32!SendInput` による相対移動 `+1px` と `-1px` の送信 | Windows の入力 API だけを隠蔽 |
| `StartupRegistration` | HKCU Run キーの照会・登録・解除・更新後のパス補正 | Windows レジストリと Velopack の配置規則を隠蔽 |
| `TrayMenuController` | ViewModel の状態をネイティブメニューと tooltip へ投影 | 状態を所有せず、変更通知を購読して同期 |
| `WindowRestoreCoordinator` | UI 準備前を含むウィンドウ復帰要求の保留と受け渡し | lock 配下で要求と登録の競合を直列化 |
| `AcrylicFallbackHelper` | 透過効果無効時と RDP 時の不透明背景への切替 | Windows の表示設定を UI リソースへ変換 |
| `tests/IRUZ.Tests` | ViewModel、トレイ、復帰、レジストリ、タイマー競合、表示判定の回帰検証 | Avalonia Headless と純粋化した判定関数を利用 |
| `web/` | `/` と `/index.html` のランディングページ配信 | その他のパスは加工せず R2 origin へ委譲 |

## 起動・更新・復帰フロー

1. `Program.Main` は AppUserModelID を設定し、Velopack のブートストラップを最初に実行する。
2. `--restore-window` をアプリ引数から取り除き、復帰要求として `WindowRestoreCoordinator` に保留する。
3. 名前付き復帰イベントを作成してから名前付き mutex を取得する。二つ目以降のプロセスはイベントを signal して終了する。
4. 最初のプロセスは復帰イベントの監視を開始し、受け取った復帰処理を Avalonia UI スレッドへ post する。
5. Velopack の `win` channel を `https://iruz.kagayoi.com` へ問い合わせる。更新確認は 10 秒、ダウンロードは 60 秒で打ち切り、失敗時は現行版の起動を続ける。
6. 更新を適用して再起動するとき、保留中の復帰要求があれば `--restore-window` を次のプロセスへ渡す。
7. Avalonia 起動後に `App` が ViewModel と MainWindow を一つずつ作り、ウィンドウをトレイへ最小化した状態でジグルを自動開始する。`Loaded` 後に復帰処理を登録し、それ以前の保留要求を消化する。

この順序により、Velopack 専用引数を正常に処理しつつ、二重起動直後や更新再起動中の「表示」要求も失わない。

## ジグルと自動解除のデータフロー

1. ViewModel が選択間隔から `System.Timers.Timer` を作り、現在のタイマー参照と入力許可を `_jiggleGate` 配下で更新する。
2. Elapsed コールバックは、そのタイマーが現在の参照と一致し、入力が許可されている場合だけ `MouseJiggleHelper.Jiggle` を呼ぶ。
3. `SendInput` は相対移動を `+1px`、続けて `-1px` 送る。カーソルを元の位置へ戻しながら OS の最終入力を更新する。
4. 送信結果は UI スレッドへ post し、現在のタイマーと一致する場合だけ状態表示へ反映する。古いタイマーの遅延結果は破棄する。
5. 自動解除を選ぶと、選択時点からの絶対時刻を `AutoStopAt` に保持し、1 秒周期で残り時間を表示する。スリープなどで tick が欠けても、現在時刻が期限以上なら解除する。
6. 期限到達時はジグルを停止し、カウントダウンを破棄して、選択を「なし」へ戻す。再開時に以前の自動解除設定は引き継がない。

メインウィンドウの binding と `TrayMenuController` は同じ ViewModel を参照するため、開始状態、間隔、自動解除、カウントダウン、スタートアップ登録は両 UI に同じ値で現れる。

## ウィンドウと終了のライフタイム

- Avalonia の `ShutdownMode` は `OnExplicitShutdown`。タイトルバーの閉じる操作と最小化はウィンドウを隠してタスクバーから外すが、ViewModel とジグルタイマーは維持する。
- トレイアイコンのクリック、メニューの「表示」、二重起動イベントは、同じ復帰処理でウィンドウを表示・通常化・activate する。
- 明示的な「終了」または OS の shutdown request だけがアプリ終了へ進む。
- Exit 時にウィンドウ状態の購読、トレイアイコン、トレイメニューの ViewModel 購読、ViewModel のタイマーを解放する。

## 重要な不変条件

- 停止完了後は、既に発火していたタイマーからもマウス入力を送らない。
- 現在のタイマー以外の入力結果は UI 状態を上書きしない。
- タイマーのバックグラウンドコールバックから Avalonia のプロパティを直接更新しない。
- 自動解除の期限到達後は `IsRunning == false`、`AutoStopAt == null`、自動解除の選択は「なし」とする。
- ウィンドウを隠す操作とプロセスを終了する操作を分離し、トレイ常駐中もジグルを継続する。
- メイン画面とトレイメニューは一つの ViewModel を状態の正本とする。
- 復帰要求は UI の `Loaded` 前、二重起動、更新再起動のいずれでも失わない。
- 更新、シェル連携、スタートアップ照会の失敗だけではアプリ起動を妨げない。
- スタートアップ登録は文字列値として扱い、IRUZ 本体以外のプロセスから利用者の登録パスを書き換えない。

## 採用した設計判断

### `SendInput` による往復移動

`SetCursorPos` は最終入力時刻を更新しないため、`MOUSEEVENTF_MOVE` の相対入力を二回送る。実カーソル位置を維持しながら離席判定へ作用できる一方、Windows API に依存するため対象 OS を Windows に限定している。

### タイマーと UI スレッドの分離

ジグル周期とカウントダウンには `System.Timers.Timer` を使い、UI 更新だけ Dispatcher へ渡す。UI が隠れていても動作を続けられる一方、停止済みコールバックが残り得るため、入力ロックとタイマー同一性の照合を必須としている。

### 絶対時刻による自動解除

残り tick 数ではなく期限を保持する。スリープやスケジューリング遅延に強い一方、表示更新と解除判定のたびに現在時刻との差を計算する。

### 明示終了型のトレイ常駐

ウィンドウの可視性をアプリの生存期間から切り離す。誤って閉じてもジグルを継続できる一方、終了経路で購読とネイティブ資源を明示的に解放する必要がある。

### STA を維持する同期的な更新処理

更新処理を Avalonia 起動前の STA スレッド上で完了させる。起動前に最新版へ揃えられる一方、ネットワーク待ちが起動時間へ影響するため、確認とダウンロードに個別の上限を設けている。

### Native AOT とローカル署名リリース

自己完結した win-x64 Native AOT として配布し、Velopack の成果物を Certum SimplySign で署名する。利用者側の .NET 導入を不要にできる一方、署名にはローカルの SimplySign 接続が必要なので CI ではなく `scripts/release-local.ps1` がリリースを担う。

成果物は R2 の `iruz-updates` へ置き、固定 URL は upload 後に cache purge する。バージョン付き成果物は直近 2 バージョンを保持する。

### ランディング Worker と更新配信の同居

`iruz.kagayoi.com` の Worker route は `/` と `/index.html` だけを HTML 応答にし、その他を同一ホストの R2 origin へ委譲する。単一ホストで案内ページと Velopack 配信を両立できる一方、更新ファイルの Range、条件付き要求、Content-Type を壊さないよう Worker では加工しない。

### アクリルの明示的フォールバック

OS がアクリル適用を報告していても、Windows の透過無効時や RDP では不透明背景へ切り替える。共有テーマブラシへ binding することで、フォールバック中も OS のライト／ダーク設定へ追従する。
